#include <stdio.h>

/**
 * main - prints the lowercase alphabet in reverse,
 * followed by a new line
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;
	char c;

	for (i = 97; i <= 122; i++)
	{
		putchar(i);
	}
	for (i = 'a' c <= 66; c++)
	{
		putchar(c);
	}
	putchar('\n');
	return (0);
}
