#include <stdio.h>

/**
 * main - prints the lowercase alphabet in reverse,
 * followed by a new line
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 30; i <= 39; i++)
	{
		putchar(i);
	}
	for (i = 61; i <= 66; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
