#include <stdio.h>

/**
 * main - prints the lowercase alphabet in reverse,
 * followed by a new line
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;
	char c;

	for (i = 48; i <= 58; i++)
	{
		putchar(i);
	}
	for (i = 'a' c <= 'f'; c++)
	{
		putchar(c);
	}
	putchar('\n');
	return (0);
}
