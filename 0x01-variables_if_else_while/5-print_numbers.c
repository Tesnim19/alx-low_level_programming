#include <stdio.h>

/**
 * main - Entry point
 * print all 1 digit number
 *
 * Return - Always 0 if sucessfule
 *
 */
int main(void)
{
	int n = 0;

	while (n <= 9)
	{
		printf(n);
		n++;
	}
	printf('\n');
	return (0);
}
